{
  "name": "ow-assistant-edge-extension",
  "version": "0.1.0",
  "description": "Edge extension scaffold (crxjs) to integrate OpenWebUI authenticated session and remote LLM agents",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^1.0.0",
    "vite": "^4.0.0",
    "playwright": "^1.40.0"
  }
}